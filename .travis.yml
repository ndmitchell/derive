env:
# - GHCVER=7.2.2 # not supported by haskell-src-exts
# - GHCVER=7.4.2 # fails the test suite, still works though
# - GHCVER=7.6.3 # generates a diff after generation (no idea why)
# - GHCVER=7.8.4 # generates a diff after generation (no idea why)
 - GHCVER=7.10.3
 - GHCVER=8.0.2
 - GHCVER=head

matrix:
  allow_failures:
   - env: GHCVER=head

script:
 - wget https://raw.github.com/ndmitchell/neil/master/travis.sh -O - --no-check-certificate --quiet | sh
